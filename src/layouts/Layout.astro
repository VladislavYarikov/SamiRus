---
import "@/styles/base.css";
import { Image } from "astro:assets";

import headerBg from "@/assets/landing/header-bg.png";
import CommonNav from "@/components/CommonNav.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const pathname = canonicalURL.pathname;

const SearchParams = Object.fromEntries(Astro.url.searchParams.entries());

const tagId = import.meta.env.PUBLIC_YM_TAG_ID

// Track User-Agent
const userAgent = Astro.request.headers.get('User-Agent');
const ip = Astro.clientAddress;
---

<script type="text/partytown" define:vars={{ tagId, userAgent, ip}}>  
  // Function to track goals
  window.trackGoal = async (goalName, params = {}, options = {
    ym: true,
    fetch: true,
  }) => {
        console.log('trackGoal', goalName);
        
        if (window.ym) {
            window.ym(tagId, 'reachGoal', goalName)
        }

        if (options.fetch) {
          // Track Previous Page
          const previousPage = document.referrer;
          
          fetch('http://localhost:3000/', {
              
              method: 'POST',
              headers: {
                  'Content-Type': 'application/json'
              },
              body: JSON.stringify({
                goalName,
                params,
                ip,
                userAgent,
                previousPage
              })
          })
        }
    }

  // Yandex Metrika
  (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   m[i].l=1*new Date();
   for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
   k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
   (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

   ym(tagId, "init", {
        clickmap:true,
        trackLinks:true,
        accurateTrackBounce:true,
        webvisor:true
   });
   
  window.trackGoal('VISIT', {}, {
    ym: true,
    fetch: true,
  });
</script>

<html lang="ru">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <link rel="canonical" href={canonicalURL} />

    <!-- Primary Meta Tags -->
    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />

    <meta
      name="keywords"
      content="аренда спецтехники москва, спецтехника московская область, аренда строительной техники москва, аренда техники, мобильное приложение аренды, трактор, автокран, экскаватор, автовышка"
    />

    <!-- Other meta tags -->
    <slot name="meta" />  
  </head>
  
  <div><img src="https://mc.yandex.ru/watch/98817597" style="position:absolute; left:-9999px;" alt="" /></div>

  <body style="--header-height: 64px">
    <header class="relative h-[var(--header-height)] w-full flex items-center">
      <a href="/">
        <span class="relative z-10 text-accent font-bold text-4xl ml-8">
          Сами
        </span>
        <span class="text-accent text-xl relative z-10"
          >аренда спецтехники
        </span>
      </a>
      <div class="absolute z-0 inset-0">
        <Image
          class="size-full object-cover object-bottom"
          src={headerBg}
          alt="Фоновое изображение"
          loading="lazy"
          decoding="async"
        />
      </div>

      <CommonNav className="seo-hidden" />
    </header>

    <slot />

    <footer class="bg-gray-100 text-gray-600 py-8 px-4 md:px-8">
      <div class="max-w-6xl mx-auto">
        <div class="grid gap-8 mb-8">
          <!-- CommonNav -->
          <CommonNav
            className="grid grid-cols-1 tablet:grid-cols-2 laptop:grid-cols-3 desktop:grid-cols-4 gap-4"
          />

          <!-- Other links -->
          <div
            class="grid grid-cols-1 tablet:grid-cols-2 laptop:grid-cols-3 desktop:grid-cols-4 gap-4"
          >
            <a
              href="/legal/user-agreement.pdf"
              target="_blank"
              class="hover:text-blue-600 transition duration-300"
            >
              Пользовательское соглашение
            </a>
            <a
              href="/legal/privacy-policy.pdf"
              target="_blank"
              class="hover:text-blue-600 transition duration-300"
            >
              Политика конфидициальности
            </a>
            <a
              href="/legal/public-offer.pdf"
              target="_blank"
              class="hover:text-blue-600 transition duration-300"
            >
              Публичная оферта
            </a>
            <a
              href="/legal/digital-services.pdf"
              target="_blank"
              class="hover:text-blue-600 transition duration-300"
            >
              Список цифровых услуг
            </a>
          </div>
        </div>

        <div class="text-center text-sm">
          <p>
            © 2021 Сами. Почта <a
              href="mailto:support@sami.plus"
              class="hover:text-blue-600 transition duration-300"
            >
              support@sami.plus
            </a>
          </p>
        </div>
      </div>
    </footer>
  </body>
</html>
